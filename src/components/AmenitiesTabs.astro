---
const restaurantData = {
  title: "Mipeja Signature Restaurant",
  description: "Indulge in a culinary journey at Mipeja's signature restaurant. Our space combines elegant interiors with a warm, inviting atmosphere, perfect for breakfast meetings, casual lunches, or romantic dinners. We take pride in serving fresh, locally-sourced ingredients prepared with international flair.",
  images: [
    "/images/twentyone.webp",
    "/images/twentytwo.webp",
    "/images/twentythree.webp"
  ],
  foodMenu: [
    { name: "Grilled Tilapia", price: "KSh 1,200", description: "Fresh lake tilapia served with ugali and traditional greens." },
    { name: "Mipeja Beef Stew", price: "KSh 950", description: "Slow-cooked tender beef in a rich, aromatic gravy." },
    { name: "Chicken Biryani", price: "KSh 1,100", description: "Fragrant rice layered with spiced chicken and caramelised onions." },
    { name: "Vegetable Stir-fry", price: "KSh 800", description: "Crispy seasonal vegetables tossed in a light soy ginger sauce." }
  ],
  drinksMenu: [
    { name: "Fresh Tropical Juice", price: "KSh 350", description: "Mango, Pineapple, or Watermelon blend." },
    { name: "Kenyan AA Coffee", price: "KSh 300", description: "Freshly brewed premium highland coffee." },
    { name: "Signature Cocktail", price: "KSh 850", description: "A refreshing blend of local spirits and exotic fruits." },
    { name: "Masala Tea", price: "KSh 250", description: "Traditional spiced Kenyan tea with milk." }
  ]
};

const conferenceData = {
  title: "Executive Conference Center",
  description: "Our modern conference facilities are designed to inspire productivity and collaboration. Equipped with high-speed WiFi, state-of-the-art audiovisual equipment, and ergonomic seating, it's the ideal venue for workshops, seminars, and corporate strategy sessions.",
  capacity: "Up to 100 People",
  images: [
    "/images/twentyfour.webp",
    "/images/twentyfive.webp",
    "/images/twentysix.webp"
  ],
  features: [
    "High-speed Fiber WiFi",
    "Projector & Motorized Screen",
    "Professional Sound System",
    "Air Conditioning",
    "Coffee & Snack Catering",
    "Dedicated Event Coordinator"
  ]
};
---

<section class="py-12 sm:py-16 md:py-24 bg-theme-off-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6">
    <!-- Tab Navigation -->
    <div class="flex justify-center mb-12 sm:mb-16 overflow-x-auto pb-4 md:pb-0 -mx-4 px-4 sm:mx-0 sm:px-0">
      <div class="inline-flex p-1 bg-gray-200/50 rounded-xl border border-gray-200 min-w-max">
        <button
          id="tab-restaurant"
          class="px-6 sm:px-8 py-2.5 sm:py-3 rounded-lg text-xs sm:text-sm font-bold uppercase tracking-widest transition-all duration-300 bg-theme-gold text-white shadow-lg whitespace-nowrap"
          data-tab="restaurant"
        >
          Restaurant
        </button>
        <button
          id="tab-conference"
          class="px-6 sm:px-8 py-2.5 sm:py-3 rounded-lg text-xs sm:text-sm font-bold uppercase tracking-widest transition-all duration-300 text-theme-black/60 hover:text-theme-black whitespace-nowrap"
          data-tab="conference"
        >
          Conference
        </button>
      </div>
    </div>

    <!-- Content Sections -->
    <div id="content-restaurant" class="tab-content transition-all duration-500 opacity-100 translate-y-0">
      <div class="grid lg:grid-cols-2 gap-8 sm:gap-12 md:gap-16 items-center mb-16 sm:mb-20 md:mb-24">
        <div class="space-y-6 sm:space-y-8">
          <div class="space-y-3 sm:space-y-4">
            <p class="text-[10px] uppercase tracking-[0.3em] sm:tracking-[0.4em] text-theme-gold font-bold">Culinary Excellence</p>
            <h2 class="text-3xl sm:text-4xl md:text-5xl font-display text-theme-black italic">{restaurantData.title}</h2>
          </div>
          <p class="text-theme-black/70 leading-relaxed text-base sm:text-lg">
            {restaurantData.description}
          </p>
          <div class="grid grid-cols-3 gap-3 sm:gap-4">
            {restaurantData.images.map((img, i) => (
              <div class="aspect-square overflow-hidden rounded-lg sm:rounded-xl">
                <img src={img} alt="Restaurant" class="w-full h-full object-cover hover:scale-110 transition-transform duration-500" />
              </div>
            ))}
          </div>
        </div>
        
        <div class="bg-white p-6 sm:p-8 md:p-10 rounded-2xl sm:rounded-3xl shadow-soft border border-theme-off-white/50">
          <div class="text-center mb-8 sm:mb-10 pb-4 sm:pb-6 border-b border-theme-off-white/30">
            <h3 class="text-xl sm:text-2xl font-display text-theme-black italic mb-2">The Menu</h3>
            <p class="text-[10px] uppercase tracking-[0.2em] text-theme-black/40">Savor the Flavors of Mipeja</p>
          </div>
          
          <div class="space-y-8 sm:space-y-10">
            <div>
              <h4 class="text-xs uppercase tracking-[0.3em] text-theme-gold font-bold mb-6">Food Specialties</h4>
              <div class="space-y-6">
                {restaurantData.foodMenu.map(item => (
                  <div class="flex justify-between gap-4">
                    <div>
                      <div class="flex items-center gap-2 mb-1">
                        <span class="text-sm font-bold text-theme-black">{item.name}</span>
                        <div class="flex-grow border-b border-dotted border-theme-off-white border-width-2"></div>
                      </div>
                      <p class="text-xs text-theme-black/50 leading-relaxed">{item.description}</p>
                    </div>
                    <span class="text-sm font-bold text-theme-black whitespace-nowrap">{item.price}</span>
                  </div>
                ))}
              </div>
            </div>

            <div>
              <h4 class="text-xs uppercase tracking-[0.3em] text-theme-gold font-bold mb-6">Signature Drinks</h4>
              <div class="space-y-6">
                {restaurantData.drinksMenu.map(item => (
                  <div class="flex justify-between gap-4">
                    <div>
                      <div class="flex items-center gap-2 mb-1">
                        <span class="text-sm font-bold text-theme-black">{item.name}</span>
                        <div class="flex-grow border-b border-dotted border-theme-off-white"></div>
                      </div>
                      <p class="text-xs text-theme-black/50 leading-relaxed">{item.description}</p>
                    </div>
                    <span class="text-sm font-bold text-theme-black whitespace-nowrap">{item.price}</span>
                  </div>
                ))}
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div id="content-conference" class="tab-content hidden opacity-0 translate-y-10 transition-all duration-500">
      <div class="grid lg:grid-cols-2 gap-8 sm:gap-12 md:gap-16 items-center">
        <div class="relative group">
          <div class="grid grid-cols-2 gap-3 sm:gap-4">
            <div class="space-y-3 sm:space-y-4">
              <div class="aspect-[4/5] rounded-2xl sm:rounded-3xl overflow-hidden shadow-lg">
                <img src={conferenceData.images[0]} alt="Conference 1" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700" />
              </div>
              <div class="aspect-square rounded-2xl sm:rounded-3xl overflow-hidden shadow-lg">
                <img src={conferenceData.images[1]} alt="Conference 2" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700 delay-100" />
              </div>
            </div>
            <div class="pt-8 sm:pt-12">
              <div class="aspect-[4/6] rounded-2xl sm:rounded-3xl overflow-hidden shadow-lg">
                <img src={conferenceData.images[2]} alt="Conference 3" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700 delay-200" />
              </div>
            </div>
          </div>
          <!-- Capacity Badge -->
          <div class="absolute -bottom-4 sm:-bottom-6 -right-4 sm:-right-6 bg-theme-black text-white p-6 sm:p-8 rounded-full shadow-2xl flex flex-col items-center justify-center animate-bounce-slow">
            <span class="text-2xl sm:text-3xl font-display italic leading-none">{conferenceData.capacity.split(' ')[2]}</span>
            <span class="text-[7px] sm:text-[8px] uppercase tracking-widest font-bold">Capacity</span>
          </div>
        </div>

        <div class="space-y-6 sm:space-y-8 md:space-y-10">
          <div class="space-y-3 sm:space-y-4">
            <p class="text-[10px] uppercase tracking-[0.3em] sm:tracking-[0.4em] text-theme-gold font-bold">Business & Events</p>
            <h2 class="text-3xl sm:text-4xl md:text-5xl font-display text-theme-black italic">{conferenceData.title}</h2>
          </div>
          <p class="text-theme-black/70 leading-relaxed text-base sm:text-lg">
            {conferenceData.description}
          </p>
          
          <div class="grid sm:grid-cols-2 gap-4 sm:gap-6 pt-4 sm:pt-6">
            {conferenceData.features.map(feature => (
              <div class="flex items-center gap-3 sm:gap-4 group">
                <div class="w-7 h-7 sm:w-8 sm:h-8 rounded-full bg-theme-off-white flex items-center justify-center text-theme-gold group-hover:bg-theme-gold group-hover:text-white transition-colors duration-300 flex-shrink-0">
                  <svg class="w-3.5 h-3.5 sm:w-4 sm:h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                </div>
                <span class="text-xs sm:text-sm font-bold uppercase tracking-widest text-theme-black/70">{feature}</span>
              </div>
            ))}
          </div>

          <div class="pt-6 sm:pt-8">
            <a href="/contact" class="inline-flex items-center justify-center bg-theme-black hover:bg-theme-black/90 text-white px-8 sm:px-10 py-3 sm:py-4 rounded-xl text-xs sm:text-sm font-bold uppercase tracking-widest transition-all duration-300 shadow-lg hover:-translate-y-1">
              Inquire Now
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  function initTabs() {
    const tabs = document.querySelectorAll('[data-tab]');
    const contents = document.querySelectorAll('.tab-content');

    tabs.forEach(tab => {
      tab.addEventListener('click', () => {
        const target = tab.getAttribute('data-tab');

        // Update active tab styling
        tabs.forEach(t => {
          t.classList.remove('bg-theme-gold', 'text-white', 'shadow-lg');
          t.classList.add('text-theme-black/60');
        });
        tab.classList.add('bg-theme-gold', 'text-white', 'shadow-lg');
        tab.classList.remove('text-theme-black/60');

        // Update content visibility
        contents.forEach(content => {
          if (content.id === `content-${target}`) {
            content.classList.remove('hidden');
            setTimeout(() => {
              content.classList.remove('opacity-0', 'translate-y-10');
              content.classList.add('opacity-100', 'translate-y-0');
            }, 10);
          } else {
            content.classList.add('opacity-0', 'translate-y-10');
            content.classList.remove('opacity-100', 'translate-y-0');
            setTimeout(() => content.classList.add('hidden'), 500);
          }
        });
      });
    });
  }

  // Initialize
  initTabs();
  document.addEventListener('astro:after-swap', initTabs);
</script>

<style>
  .animate-bounce-slow {
    animation: bounce 3s infinite;
  }
  @keyframes bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
</style>
